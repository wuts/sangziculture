<div id="top_menu">
  <div id="site_menu">
  <ul>
    <li class="current"><%= link_to "桑榇布艺About",about_url %></li>
    <li class="ff"><%= link_to "<span>产品展示Product</span>",products_url %></li>
    <li><%= link_to "合作共赢Win/Win",corporation_url %></li>
    <li><%= link_to "乐淘淘Taobao",taotao_url %></li>
    <li><%= link_to "心情涂鸦Story",products_url %></li>
    <li><%= link_to "联系我们contact",contact_url %></li>
  </ul>
  <div class="clear"></div>
  </div>
  <div id="category_menu">
  <ul>
    <% @root_taxons.each do |taxon| %>
    <li><%= link_to taxon.name,seo_url(taxon) %></li>
    <% end %>
  </ul>
  </div>
   <div id="subcategory_menu">

     <% if @taxon %>
        <% if @taxon.children.empty? %>

        <ul>
         <% @taxon.parent.children.each do |child_taxon| %>

         <li><%= link_to child_taxon.name,seo_url(child_taxon) %></li>
       <% end %>
      </ul>

    <% else %>
      <ul>
    <% @taxon.children.each do |child_taxon| %>

      <li><%= link_to child_taxon.name,seo_url(child_taxon) %></li>
    <% end %>
    </ul>
      <% end %>
  <% else %>
      <ul>
         <% @root_taxons.first.children.each do |child_taxon| %>

         <li><%= link_to child_taxon.name,seo_url(child_taxon) %></li>
       <% end %>
      </ul>

  <% end %>
    </div>
</div>


